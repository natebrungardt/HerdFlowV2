# HerdFlow iOS

This directory is reserved for the future iOS app.

Planned structure:

- `apps/web` for the current React web app
- `apps/ios` for the future iOS client
- `services/api` for the shared backend API

If payments are added later, Stripe should start in `services/api` unless billing becomes a separate service with its own deployment lifecycle.
